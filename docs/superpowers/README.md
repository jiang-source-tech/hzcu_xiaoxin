# Superpowers Docs Archive

This directory stores historical specs and implementation plans.

Relationship-test documents under `specs/` and `plans/` are archived. The current project direction is:

- Use `/test` for daily Xiaoxin semantic review and optimization.
- Keep `/relationship-test` disabled.
- Keep relationship self-play Web/API/CLI disabled.
- Do not execute historical plans that ask an agent to re-enable relationship-test, run all scenes, or start dual-LLM relationship self-play.

The old documents remain only as design history and background context.
